void main(List<String> args) {
  int money = 5;

  money += 1;

  money++;

  String name = 'veli';
  String surname = 'bacik';

  print(name + ' ' + surname);

  if(name == 'veli'){}
  if(surname != 'veli'){}
  if(name.length > 'veli'.length);
  if(name.length < 'veli'.length);
  if(name.length >= 'veli'.length);
  if(name.length <= 'veli'.length);

const int appleMoney = 20;
const double disccount = 2.5;

  int myMoney = 30;
  myMoney -= (appleMoney / disccount).toInt();

  print(myMoney);

  print(myMoney % 2 == 0);
}